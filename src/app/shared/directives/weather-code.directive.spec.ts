import { WeatherCodeDirective } from './weather-code.directive';

describe('WeatherCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherCodeDirective();
    expect(directive).toBeTruthy();
  });
});
